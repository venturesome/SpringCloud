server:
  port: 8781
  
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      homePageUrl: https://${eureka.instance.hostname}/
      
spring:
  application:
    name: ribbonService